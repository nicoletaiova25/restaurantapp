name: docker-build-and-push-removed

# This workflow was intentionally removed. It remains as a no-op placeholder to avoid CI schema warnings.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op placeholder
        run: |
          echo "Docker CI removed: this workflow is a placeholder and performs no actions."
